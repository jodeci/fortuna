%h1 建立薪資資料

= simple_form_for @employee, url: employee_salaries_path, method: :post do |f|
  = render "/employees/info", employee: @employee
  = render "form", salary: @salary

  .text-center
    = f.submit "送出"
